<h1>Welcome to the checklist index page</h1>
<% if @user.checklists.any? %>
  <% @checklists.each do |checklist| %>
    <%= checklist.created_at %>
  <% end %>
<% else %>
  <p>You have no checklists yet.</p>
<% end %>